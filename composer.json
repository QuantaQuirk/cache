{
    "name": "quantaquirk/cache",
    "description": "The QuantaQuirk Cache package.",
        "license": "proprietary",
    "authors": [
        {
            "name": "Tomáš Selič (Smisch-DEV)",
            "email": "contact@smisch.sk"
        }
    ],
    "require": {
        "php": "^8.1",
        "quantaquirk/collections": "^10.0",
        "quantaquirk/contracts": "^10.0",
        "quantaquirk/macroable": "^10.0",
        "quantaquirk/support": "^10.0"
    },
    "provide": {
        "psr/simple-cache-implementation": "1.0|2.0|3.0"
    },
    "autoload": {
        "psr-4": {
            "QuantaQuirk\\Cache\\": ""
        }
    },
    "extra": {
        "branch-alias": {
            "dev-master": "10.x-dev"
        }
    },
    "suggest": {
        "ext-apcu": "Required to use the APC cache driver.",
        "ext-filter": "Required to use the DynamoDb cache driver.",
        "ext-memcached": "Required to use the memcache cache driver.",
        "quantaquirk/database": "Required to use the database cache driver (^10.0).",
        "quantaquirk/filesystem": "Required to use the file cache driver (^10.0).",
        "quantaquirk/redis": "Required to use the redis cache driver (^10.0).",
        "symfony/cache": "Required to use PSR-6 cache bridge (^6.2)."
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev"
}
